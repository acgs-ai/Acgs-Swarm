# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-21 11:45 → 2026-07-22 12:16 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-22 12:16 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- **CI is broken on `main`:** `verify-citations` failed on the latest main
  commit (SHA `1ecc0dfc`, 2026-07-20); all three CI workflows
  (`CI`, `agent-check`, `verify-citations`) are also failing on every
  `pulse/*` branch — 6/6 runs in the 24h window are failures.
- **No commits to `main` in 17 days:** last substantive activity (style fix
  + bittensor feat) landed 2026-07-05; main has been frozen since.
- **Pulse-PR backlog now at 15 open drafts** (2026-07-06 → 2026-07-21), none
  merged; two unreviewed Dependabot CI-action bumps (#137, #138) and one
  16-day-old braintrust build bump (#122) also sit unmerged.

## Activity

- Commits: 0 in 24h · 0 in 7d · 23 in 30d (latest main commit: 2026-07-05).
- Authors: 0 in 24h/7d · 3 in 30d.
- 30d line churn: +1,639 / −89 — ext mix: 16 `.md`, 12 `.py`, 7 `.yml`,
  1 `.toml`; ⚠️ `.md` plurality reflects 15+ unmerged pulse-report branches
  inflating doc churn vs authored code.
- 30d commit types: 16 docs · 2 feat · 2 ci · 1 style · 1 fix · 1 build.

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 126 collected (5.53s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 79 collection errors all trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (9 TODO, 1 XXX — unchanged).
- CI runs in 24h window: **6 failure, 0 success** — all on `pulse/2026-07-21`
  branch; `CI`, `agent-check`, and `verify-citations` each ran twice and failed.
- Latest main-branch CI: `verify-citations` — **FAILURE** — 2026-07-20T08:01 UTC
  on SHA `1ecc0dfc` (HEAD). Main is **not green**.
- All 30 most-recent runs across all branches: ~24 failures, 5 success (all
  success runs are Dependabot graph-update jobs, not the primary CI suite).
- Open PRs: 18 total — 15 stale pulse drafts (#123–#139, 2026-07-06 →
  2026-07-21); 2 Dependabot CI-action bumps (#137 `setup-python` 6→7, #138
  `gh-action-pypi-publish` 1.14.0→1.14.1, both 2 days old); 1 Dependabot
  build bump (#122 `braintrust` 0.25→0.26, 16 days old).
- Working tree: clean.

## Followups

1. **CI broken on main — `verify-citations` failing (SHA `1ecc0dfc`,
   2026-07-20):** This is the top priority. The failure is also reproducing
   on every `pulse/*` branch push, suggesting a systemic issue (broken
   citation/reference check, a removed link, or a changed dependency) rather
   than something branch-specific. Investigate and push a fix to `main`.
2. **15 stale pulse-report PRs (#123–#139) with no merge policy:** The backlog
   grows by one every day the scheduled task runs. An auto-merge rule on
   `pulse/*` branches (or a batch-close action) would prevent indefinite
   accumulation.
3. **3 non-pulse PRs unreviewed:** Dependabot bumps #137 and #138 (2 days,
   CI-action version bumps — low risk) and #122 (16 days, `braintrust`
   dependency — worth triaging for breakage risk given the current CI
   failures).
