# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-27 12:09 → 2026-07-28 12:24 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-28 12:24 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- No commits to `main` in the 24h window; latest main commit (`68a661a9`,
  2026-07-27) bounds `ruff (<0.16)` and `bittensor (<11)` to restore CI gates —
  all four core workflows green on that SHA.
- Open PR count reached **20**, of which 16 are unmerged draft pulse reports
  spanning 2026-07-10 → 2026-07-27; previous pulses flagged this pattern at 2
  PRs, now at 16 with no merge/close policy in evidence.
- A Renovate-style uv.lock auto-update workflow failed on the latest main SHA
  (`68a661a9`) — distinct from core CI, which is entirely green.

## Activity

- Commits: 0 in 24h · 3 in 7d · 26 in 30d.
- Authors: 0 in 24h · 3 in 7d · 5 in 30d.
- 24h line churn: none.
- 7d line churn: +27 / −18 — 3 commits: 1 build (ruff/bittensor version pins),
  1 style (RUF036 type-union reorder), 1 Blacksmith runner migration.
- 30d line churn: +1,666 / −107 — file-type mix: md +1,079 / −0 (bulk from 16
  `docs(pulse)` commits — ⚠️ skews raw churn; authored `.py` is +546 / −75),
  py +546 / −75, yaml +29 / −29, other +12 / −3.
- 30d commit types: 16 docs · 2 style · 2 feat · 2 ci · 2 build · 1 fix.

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 126 collected (3.75s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 79 collection errors all trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- CI on latest main SHA (`68a661a9`): **CI** success · **agent-check** success
  · **verify-citations** success · **security-regression** success — fully green.
  One automated `uv in /.` update workflow failed on the same SHA (Renovate-style
  dependency bot, not a authored-code failure).
- CI runs in 24h window: 3 success, 0 failure — all on `pulse/2026-07-27` branch.
  No direct pushes to `main` triggered CI in the window.
- Open PRs: 20 — 16 draft pulse reports (2026-07-10 → 2026-07-27, #127–#148),
  2 non-draft dependency bumps (#137 `actions/setup-python`, #138
  `pypa/gh-action-pypi-publish`), 1 non-draft build fix (#146 sync uv.lock).
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (9 TODO, 1 XXX — unchanged across
  multiple pulses).

## Followups

1. **Pulse PR accumulation now critical (16 open draft PRs):** The backlog of
   unmerged pulse draft PRs grew from 2 (noted 2026-07-05) to 16. A bulk-close
   script or auto-merge label on `pulse/*` branches is overdue; the accumulation
   will obscure real PRs and inflate PR metrics each week it continues.
2. **Non-draft #146 (sync uv.lock + testnet-deploy hint) needs triage:** Opened
   alongside the CI-gate fix (#147, now merged to main) — if the uv.lock sync is
   intentional, merge or close it before the lockfile drifts further.
3. **Renovate uv.lock update failing on `main`:** The `uv in /.` automated
   dependency-update workflow failed on `68a661a9`. Core CI is unaffected, but a
   persistent lockfile-update failure can cause the lock to diverge from actual
   resolved deps over time — worth inspecting the workflow logs.
4. **Non-draft dependency-bump PRs #137 and #138 are awaiting review:**
   `actions/setup-python` v6→v7 and `pypa/gh-action-pypi-publish` bumps are
   low-risk CI-tooling updates — quick to review, may unblock the failing uv
   update workflow.
