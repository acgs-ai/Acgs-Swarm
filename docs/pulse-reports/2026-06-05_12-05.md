# Repo Health Pulse — constitutional-swarm

**Window:** 2026-06-04 11:50 → 2026-06-05 12:05 UTC (24h, 15m trailing buffer)
**Run:** 2026-06-05 12:05 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- 4 commits merged in 24h (2 fix, 2 docs): security hardening (#83), wiki (#81), spectral-sphere safe default (#80), and yesterday's pulse report (#84).
- CI is green: 29 of 30 workflow runs succeeded in-window; latest main commit (#83) passed all 4 gates (CI, security-regression, agent-check, verify-citations).
- 2 open PRs awaiting merge: PR #85 (interpreter deny-list completeness, security fix) and PR #82 (lazy bittensor import, correctness fix).

## Activity

- Commits: 4 in 24h · 42 in 7d · 76 in 30d — sustained cadence.
- Authors: 1 in 24h · 3 in 7d · 4 in 30d.
- 24h file mix: 20 `.py`, 13 `.md` — authored code dominant.
- 24h line churn: +2,079 / −70.
- 7d line churn: +67,145 / −854 — ⚠️ **bulk skew**: ~295 of 411 touched files are `.md` (agent persona template drops, `agents/templates/`). Authored `.py` changes number ~87 files. Treat 7d raw churn as inflated by ~97%.
- 7d commit types: 14 feat, 8 docs, 7 fix, 5 chore, 3 build, 2 ci, 1 refactor.
- 30d commit types: 25 feat, 11 docs, 9 fix, 9 build, 6 chore, 2 ci, 1 refactor.

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 124 collected (4.70s) — not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 77 collection errors all trace to absent `acgs_lite` (monorepo-only dep, not installed in standalone checkout). Not a repo problem.
- CI runs in-window: 29 success, 1 cancelled.
- Latest main commit (7d31cd9): all 4 CI workflows green.
- Open PRs: 2 (#82, #85). Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (unchanged from prior pulse).

## Followups

1. **PR #85 (interpreter deny-list) is a security fix — merge candidate:** closes a bypass where version-suffixed interpreters (`python3.11`, `pypy3`, `uv run python`, `bash`, `node`) slipped past the governed-handoff deny-list. CI is green; ready for merge review.
2. **PR #82 (lazy bittensor import) is a correctness fix — merge candidate:** drops cold-import time by ~42% and restores clean `import constitutional_swarm` for users without the `[bittensor]` extra. CI is green.
3. **1 cancelled CI run in 24h:** pattern matches a superseded push (a new commit arrived before the prior run finished). Verify no test results were lost — not a failure indicator on its own.
4. **7d churn signal misleading:** +67k raw lines, but authored `.py` changes are ~87 files; the remainder is one-time agent template drops. Separate vendored assets from source when tracking velocity.
5. **Bus factor:** single author for all 24h commits; 3 in 7d, 4 in 30d. Neutral observation — worth monitoring if the security-hardening work stays concentrated.
