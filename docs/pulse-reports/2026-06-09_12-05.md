# Repo Health Pulse — constitutional-swarm

**Window:** 2026-06-08 12:05 → 2026-06-09 11:50 UTC (24h, 15m trailing buffer)
**Run:** 2026-06-09 12:05 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- Three commits landed in 24h — all automated (2 pulse-report merges + 1 Dependabot dep bump); no authored-code commits in-window, though 8 feat + 6 fix landed earlier in the 7-day window.
- CI fully green: 17/17 workflow runs succeeded in the 24h window; all 4 gates pass on the latest main commit (5722c939).
- Open PR queue cleared to zero — both pending items from yesterday's report (#88 pulse draft, #89 dep bump) were merged within the window.

## Activity

- Commits: 3 in 24h · 30 in 7d · 76 in 30d.
- Authors: 2 in 24h · 2 in 7d · 4 in 30d.
- 24h line churn: +88 / −1 (2 pulse-report files + 1 dep-bump toml edit — no authored `.py` changes).
- 7d line churn: +64,687 / −855 — ⚠️ **bulk skew**: 283 `.md` files vs 75 `.py` files; raw churn heavily inflated by docs/pulse-report files; authored Python changes are a small fraction.
- 30d line churn: +168,676 / −1,815 — file-type mix more balanced at this scale: 403 `.py` vs 376 `.md`, plus 18 `.tex`, 17 `.toml`, 16 `.yml`.
- 24h commit types: 2 docs, 1 build; 0 unprefixed.
- 7d commit types: 10 docs, 8 feat, 6 fix, 4 chore, 1 refactor, 1 build; 0 unprefixed.
- 30d commit types: 20 feat, 14 docs, 11 fix, 10 build, 5 chore, 2 ci, 1 refactor; 13 unprefixed (~17% of 76).

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 126 collected (4.46s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 77 collection errors all trace to absent `acgs_lite` (monorepo-only dep, not installed in standalone checkout). Not a repo problem.
- CI runs in-window (24h): 17 success, 0 failure, 0 cancelled.
- CI runs in-window (7d): 30 success, 0 failure, 0 cancelled (full page returned).
- Latest main commit (5722c939): all 4 CI workflow runs green (`CI`, `agent-check`, `security-regression`, `verify-citations`).
- Open PRs: 0 — queue fully drained (pulse drafts and dep-bump PR all merged).
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (unchanged).

## Followups

1. **30d unprefixed commits persist at ~17% (13/76) — fourth consecutive pulse flagging this:** the 7d slice shows 0 unprefixed (all recent commits are properly prefixed), so historical tail is not worsening, but the 30d figure is stale from earlier work. If commit hygiene is a priority, a pre-push hook or PR-lint job would prevent recurrence without manual convention enforcement.
2. **Zero authored-code commits in 24h again:** the 7d window shows active development (8 feat + 6 fix), but the most recent 24h contains only automated commits (pulse merges + dep bump). May indicate feature work is accumulating in local or untracked branches.
3. **7d churn still docs-dominant (283 md vs 75 py):** pulse reports and documentation are driving bulk churn; if churn metrics are used as a proxy for code activity, they will overstate it. Consider separating `docs/pulse-reports/` from churn accounting in future tooling.
