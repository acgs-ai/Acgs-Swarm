# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-22 12:00 → 2026-07-23 11:45 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-23 12:00 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- Zero commits to main in both 24h and 7d; last substantive activity was the
  ruff style cleanup (#121) merged more than 7 days ago — the repo is in a
  quiet phase following the July 5–6 feat(bittensor) + style burst.
- `verify-citations` is **failing on main** at SHA `1ecc0dfc0f` (2026-07-20) —
  the citation-check workflow was green on earlier SHAs and may have regressed
  with #120 or #121; CI on the default branch is not fully green.
- Pulse PR backlog is now critical: **17 open draft pulse PRs** spanning
  July 6–22 with no merge or close activity in 18+ days; three additional
  non-draft dependency PRs (#122, #137, #138) are also stalled.

## Activity

- Commits: 0 in 24h · 0 in 7d · 23 in 30d.
- Authors: 0 in 24h · 0 in 7d · 3 in 30d (2 human, 1 dependabot).
- 24h/7d line churn: +0 / −0 (no commits).
- 30d line churn: +1,639 / −89 — file-type mix: 16 `.md`, 12 `.py`, 7 `.yml`,
  1 `.toml`.
  - ⚠️ Churn skew: 16 of 23 commits are `docs` (pulse reports); authored `.py`
    is only 12 files, concentrated in feat(bittensor) (#120) and style
    cleanup (#121). No vendored or bulk-import drops detected in the window.
- 30d commit-type prefixes: 16 docs · 2 feat · 2 ci · 1 style · 1 fix · 1 build.

## System / Code Health

- Lint: `ruff check src/` — **all checks passed**.
- Tests: 126 collected (4.03s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 79 collection errors all trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- CI runs in 24h window: **3 runs, all failure** — `verify-citations`, `CI`,
  and `agent-check` on `pulse/2026-07-22` branch (SHA `851e47ef7a`); no
  main-branch runs in the window.
- Latest main-branch CI (SHA `1ecc0dfc0f`, 2026-07-20): `verify-citations`
  — **failure**; Dependabot updater runs on same SHA: 3 success (pip, github_actions),
  1 failure (uv multi-package update). The primary `CI` and `agent-check`
  workflows are not visible in the latest 30-run API window for main, so their
  status at this SHA is unknown.
- All 30 most-recent CI runs (all branches): 4 success, 26 failure —
  heavily weighted by stalled Dependabot update runs and pulse-branch failures.
- Open PRs: 19 total — 17 draft pulse reports (July 6–22), 2 non-draft
  dependency bumps (#137 actions/setup-python 6→7, #138
  pypa/gh-action-pypi-publish), 1 non-draft build bump (#122 braintrust).
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: **10** (9 TODO, 1 XXX — unchanged
  for 4+ consecutive pulses).

## Followups

1. **`verify-citations` failing on main (SHA `1ecc0dfc0f`, since 2026-07-20):**
   This workflow was passing on earlier SHAs; the regression likely entered
   with feat(bittensor) (#120) or the style/ruff cleanup (#121). Investigate
   the citation check failure and push a fix — CI on main should be green
   before new feature work resumes.
2. **Pulse PR backlog (17 open drafts, Jul 6–22):** No pulse PR has been
   merged or closed in 18+ days; the accumulation creates noise that buries
   actionable dependency PRs. An auto-merge or scheduled batch-close policy
   for `pulse/*` branches is overdue.
3. **Stalled dependency PRs (#122, #137, #138):** Three non-draft PRs
   (braintrust bump, actions/setup-python 6→7, pypa/gh-action-pypi-publish)
   have been open since July 6–20 with no review. Given CI instability, they
   should be unblocked once `verify-citations` is fixed on main.
4. **TODO marker plateau (10 markers, 4+ pulses unchanged):** No movement
   on TODO/XXX reduction despite recent fix and style activity — a brief
   triage pass is worth scheduling to confirm which are intentional deferrals.
