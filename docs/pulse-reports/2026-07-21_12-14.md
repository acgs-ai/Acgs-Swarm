# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-20 12:14 → 2026-07-21 11:59 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-21 12:14 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- Repo has been quiet for 16 days: zero commits in 24h or 7d; last code
  commit was `style: clear 8-error ruff baseline` on 2026-07-05.
- `verify-citations` failed on `main` in Monday's weekly scheduled run
  (2026-07-20 08:01 UTC, SHA `1ecc0dfc`) — likely a dead or rate-limited
  arXiv citation; same workflow has failed on every `pulse/*` branch since
  2026-07-15, pointing to a persistent citation-link problem.
- Open PR count has grown to 17: 14 unmerged pulse draft reports (back to
  2026-07-06) plus 2 open Dependabot CI-action bumps (#137, #138) and 1
  stale dependency PR (#122).

## Activity

- Commits: 0 in 24h · 0 in 7d · 23 in 30d.
- Authors: 0 in 24h · 0 in 7d · 3 in 30d.
- 24h / 7d line churn: none.
- 30d line churn: +1,639 / −89 across 33 files — file-type mix: 16 `.md`,
  12 `.py`, 7 `.yml`, 1 `.toml`; `.md` dominates (pulse reports + docs
  commits account for 16 of 23 commits in the window).
- 30d commit types: 16 docs · 2 feat · 2 ci · 1 style · 1 fix · 1 build.
  No vendor or bulk-import drops detected that would skew churn figures.

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 1,837 collected (3.69s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 6 collection errors trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- CI on `main` (last 30 fetched runs): 8 success, 22 failure — breakdown
  reflects pulse-branch churn (3 workflows × 8 pulse branches failing).
- `verify-citations` weekly run on `main` SHA `1ecc0dfc`: **failure**
  (2026-07-20 08:01 UTC). Logs expired (GitHub 404 on download); the actual
  observed state of the environment and citation dependencies during this run
  is unverified.
- `verify-citations` on `pulse/*` branches: failing every day since
  2026-07-15 (pulse/2026-07-14 was the last green run). Logs unavailable.
- `CI` and `agent-check` also failing on all `pulse/*` branches since
  2026-07-15 and on both Dependabot branches (#137 setup-python v6→v7,
  #138 pypi-publish bump). Notably, all CI jobs on this PR's branch
  completed in 2-3 seconds with no runner assigned (`runner_id: 0`,
  `runner_name: ""`), indicating a pre-runner infrastructure rejection
  rather than test failures. The pulse PR's single markdown addition cannot
  cause these failures; they pre-date this PR by seven days.
- Open PRs: 17 total — 14 pulse drafts (oldest: #122 braintrust bump
  2026-07-06, #123 pulse 2026-07-06), 2 Dependabot CI bumps, 1 stale
  dependency PR.
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (9 TODO, 1 XXX — unchanged
  for 3+ pulses).

## Followups

1. **`verify-citations` failing on `main` (weekly scheduled, 2026-07-20):**
   The workflow failed; logs expired before root cause could be confirmed.
   Run `python scripts/verify_citations.py --root . --timeout 15 --workers 8`
   locally to determine whether the failure is a dead citation, a transient
   network error, or something else.
2. **`CI` and `agent-check` failing on `pulse/*` and Dependabot branches:**
   Three workflows fail on every branch opened since 2026-07-15.
   Dependabot has open PRs to bump `actions/setup-python` v6→v7 (#137) and
   `pypa/gh-action-pypi-publish` 1.14.0→1.14.1 (#138) — both branches also
   fail CI. Investigate whether `actions/setup-python@v6` deprecation or a
   runner image change is the trigger; merging #137 may resolve the action
   version issue.
3. **Pulse PR backlog (14 unmerged drafts, oldest 15 days):** PRs #123–#136
   remain open as drafts. Consider adding auto-merge or a weekly batch-close
   for `pulse/*` branches to prevent continued accumulation.
