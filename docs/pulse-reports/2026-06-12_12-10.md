# Repo Health Pulse — constitutional-swarm

**Window:** 2026-06-11 12:10 → 2026-06-12 11:55 UTC (24h, 15m trailing buffer)
**Run:** 2026-06-12 12:10 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- One docs commit landed in 24h (+62 .md lines, pulse report only); 7d window shows
  10 commits from 2 authors — 7 docs, 2 fix, 1 build — consistent light but
  prefixed activity.
- CI fully green: all 4 gates (CI, agent-check, verify-citations,
  security-regression) passed on latest main commit (34e35fd); 7 runs triggered
  in the 24h window, 30/30 runs succeeded across the 7d page, 0 failures.
- 30d raw line churn (+168,910) is dominated by two bulk/vendor commits
  (98K-line benchmark-artifacts import + 55K-line agent-template vendor); these
  inflate `.py` and `.md` totals and are not a proxy for authored code velocity.

## Activity

- Commits: 1 in 24h · 10 in 7d · 70 in 30d.
- Authors: 1 in 24h · 2 in 7d · 4 in 30d.
- 24h line churn: +62 / −0 (1 docs commit, all `.md`).
- 7d line churn: +778 / −43 (`.md` 608 · `.py` 211 · `.toml` 2).
- 30d line churn: +168,910 / −1,781 — ⚠️ **bulk skew**: two commits
  account for ~153K of 168K added lines:
  - `79725f5` "Anchor the v0.1 governance benchmark" (+98K: 76K `.py`,
    8.5K `.sty`, 4.7K `.md`, 3K `.lock`, 1.7K `.tex`, 1.4K `.bst`, …) —
    benchmark artifacts + research scaffolding, not net-new feature code.
  - `6141112` "chore(agents): vendor persona agent template library (184 agents)"
    (+55K: 55K `.md` templates) — pure template vendor drop.
  Raw 30d churn is unreliable as a code-velocity proxy; the ~15K remaining lines
  across 68 other commits give a cleaner signal.
- 24h commit types: 1 docs.
- 7d commit types: 7 docs · 2 fix · 1 build — all prefixed, 0 unprefixed.
- 30d commit types: 17 docs · 14 feat · 10 fix · 9 build · 5 chore · 2 ci ·
  1 refactor · 12 unprefixed (~17.1% of 70 — improving from 18.6% last pulse).

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 126 collected (4.44s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 77 collection errors all trace to absent `acgs_lite`
    (monorepo-only dep, not installed in standalone checkout). Not a repo problem.
- CI runs in-window (24h): 7 (all success — triggered by the single 24h push).
- CI runs in-window (7d): 30 success, 0 failure, 0 cancelled.
- Latest main commit (34e35fd): all 4 CI workflow gates green.
- Open PRs: 0 — queue clear.
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 total (9 TODO · 1 XXX — unchanged).

## Followups

1. **30d unprefixed commits at ~17.1% (12/70) — improving but persistent:** the
   7d window remains fully prefixed (0/10 unprefixed), confirming no new violations
   are being introduced. The 30d figure is a historical tail; watch for it to
   continue declining as older unprefixed commits age out of the window.
2. **Bulk 30d churn needs tracking exclusion:** the two bulk commits (`79725f5`
   benchmark artifacts and `6141112` agent templates) inflate the `.py` and `.md`
   30d totals by 10×+. If code velocity is tracked, scoping `git log --numstat`
   to exclude `paper/`, `agents/templates/`, and similar non-authored-code paths
   would give a representative signal.
3. **Sustained low code-commit cadence entering second week:** only fix/build
   commits have touched `.py` in the 7d window (+211 lines total); no feat-type
   `.py` changes landed. Not necessarily a problem, but worth confirming against
   sprint expectations if a feature cycle is underway.
