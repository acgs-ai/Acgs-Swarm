# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-30 12:03 → 2026-07-31 12:03 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-31 12:18 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- No commits in the 24h window; the most recent activity was a 3-commit cluster on
  2026-07-27 (ruff/bittensor version pins, Blacksmith runner migration, style cleanup)
  — main has been quiet for 4 days.
- CI on the latest main commit (`68a661a9`) is green across all core workflows (CI,
  agent-check, verify-citations, security-regression, Graph Update); two uv
  lock-update bot runs failed on that same SHA after the pin constraints landed —
  lock file may be stale under the new bounds.
- Pulse PR accumulation continues to worsen: 24 pulse-report PRs now open (oldest
  #123, created 2026-07-06, 25 days ago), plus #146 (non-pulse fix) and 3 Dependabot
  PRs — 28 open PRs total.

## Activity

- Commits: 0 in 24h · 3 in 7d · 11 in 30d.
- Authors: 0 in 24h · 3 in 7d · 4 in 30d.
- 24h line churn: none (no commits in window).
- 7d line churn: +27 / −18.
- 30d line churn: +843 / −91 — file-type mix: `.py` +546/−75 (65% of additions),
  doc (`.md` + `.yaml` + `.toml`) +297/−16 (35%); doc share is pulse reports and
  workflow migration, not vendor bulk imports.
- 30d commit types: 4 docs · 2 style · 2 feat · 1 fix · 1 build.

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 126 collected (3.45s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 79 collection errors all trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- CI in 24h window: 3 runs, all success.
- Latest main-branch CI on SHA `68a661a9` (2026-07-27): core workflows all green
  — CI, agent-check, verify-citations, security-regression, Graph Update: uv success;
  2 uv lock-update bot runs failed on that SHA.
- All 30 most-recent CI runs: 27 success, 3 failure (2 uv lock-update bot runs on
  main; 1 CI run on `dependabot/actions/setup-python-7` branch).
- Open PRs: 28 total — 24 pulse draft reports (#123–#151), 1 non-draft fix (#146),
  3 Dependabot bumps (#122, #137, #138); several pulse PRs went non-draft without
  merging (#125–#129).
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (9 in `forensic_benchmark.py` as URI
  placeholders, 1 in `local_harness.py` docstring — unchanged from prior pulses).

## Followups

1. **Pulse PR backlog now 24 open (oldest #123, 25 days):** Accumulation is
   accelerating — nearly one unmerged pulse PR per day. Several went non-draft
   (e.g., #125–#129) without merging, burying non-pulse PR signal. An auto-merge or
   scheduled batch-close policy for `pulse/*` branches is overdue.
2. **UV lock-update bot failed twice on main (SHAs `68a661a9`, `caa7907c`):** Both
   failures hit immediately after the ruff (<0.16) and bittensor (<11) pin commits
   (#147). The uv lock file may be stale under the new bounds. Running `uv lock`
   locally, verifying, and committing the result (or merging #146, which syncs
   `uv.lock` to match the pin bounds) would clear the bot failures.
3. **CI failing on Dependabot #137 (`actions/setup-python-7` branch):** The CI
   workflow failed on the Dependabot branch for the `setup-python` v6→v7 bump —
   this PR cannot be merged as-is. Worth investigating compatibility before it ages
   further.
4. **#146 (`build: sync uv.lock with ruff/bittensor bounds`) non-draft, 4 days
   open:** This fix-up PR is likely the fastest path to resolving the uv bot failures
   noted in followup 2 — confirm the lock file diff is correct and merge.
