# Repo Health Pulse — constitutional-swarm

**Window:** 2026-06-12 12:08 → 2026-06-13 11:53 UTC (24h, 15m trailing buffer)
**Run:** 2026-06-13 12:08 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- One docs commit landed in 24h (+69 .md lines, pulse report only); 7d window shows
  8 commits from 2 authors — all docs (7) or build (1) — second consecutive week
  with zero feat/fix commits touching `.py`.
- CI fully green: all 4 gates (CI, agent-check, verify-citations,
  security-regression) passed on latest main commit (b6916f0); 7 runs in the 24h
  window and all 30 in the 7d page succeeded, 0 failures.
- 30d unprefixed commit ratio improving: 11/69 (15.9%) vs 12/70 (17.1%) last pulse;
  7d window remains fully prefixed (0/8) — no new violations being introduced.

## Activity

- Commits: 1 in 24h · 8 in 7d · 69 in 30d.
- Authors: 1 in 24h · 2 in 7d · 4 in 30d.
- 24h line churn: +69 / −0 (1 docs commit, all `.md`).
- 7d line churn: +370 / −1 (`.md` dominant: 7 commits · `.toml` 1 commit).
- 30d line churn: +168,655 / −1,554 — ⚠️ **bulk skew** persists from prior windows:
  two bulk commits still inside the 30d window account for ~153K of 168K added lines
  (benchmark-artifacts import + agent-template vendor drop); raw 30d churn remains
  an unreliable code-velocity proxy.
- 24h commit types: 1 docs.
- 7d commit types: 7 docs · 1 build — all prefixed, 0 unprefixed.
- 30d commit types: 18 docs · 14 feat · 9 fix · 9 build · 5 chore · 2 ci ·
  1 refactor · 11 unprefixed (15.9% of 69 — down from 17.1% last pulse).

## System / Code Health

- Lint: `ruff check src/` — all checks passed.
- Tests: 126 collected (3.99s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 77 collection errors all trace to absent `acgs_lite`
    (monorepo-only dep, not installed in standalone checkout). Not a repo problem.
- CI runs in-window (24h): 7 (all success).
- CI runs in-window (7d): 30 success, 0 failure, 0 cancelled.
- Latest main commit (b6916f0): all 4 CI workflow gates green.
- Open PRs: 0 — queue clear.
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 total (9 TODO · 1 XXX — unchanged).

## Followups

1. **Second consecutive week of docs-only `.py` activity:** no feat or fix commits
   touched `.py` in the 7d window (only +370 lines, all `.md`/`.toml`). The 7d
   snapshot for the prior pulse was similar (+211 `.py` lines from fix/build only).
   Worth confirming against sprint expectations if a feature cycle is in progress.
2. **30d unprefixed commits at 15.9% (11/69) — steady decline:** 7d window is
   fully prefixed for the second week. The 30d figure will continue dropping as
   older unprefixed commits age out; no action needed unless the 7d rate rises.
3. **Bulk 30d churn exclusion still relevant:** the two bulk commits (`79725f5`
   benchmark artifacts and `6141112` agent-template vendor) remain inside the 30d
   window and inflate totals ~10×. Scoping `git log --numstat` to exclude `paper/`
   and `agents/templates/` would give a representative velocity signal until these
   age out (~mid-June).
