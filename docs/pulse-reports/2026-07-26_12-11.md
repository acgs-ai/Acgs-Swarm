# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-25 12:11 → 2026-07-26 11:56 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-26 12:11 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- Main branch frozen for 21 days — zero commits in 24h or 7d; last merge was
  the `style` (#121) and `feat(bittensor)` (#120) pair on 2026-07-05.
- Local ruff 0.16.0 reports 54 errors across 13 rule categories not in the
  8-rule baseline cleared by #121 — likely a ruff version drift vs. CI; I001
  reappears despite #121's import-sort fix.
- 19 draft pulse PRs (#124–#143) have stacked up unmerged since #121, plus 2
  dependabot CI bump PRs (#137, #138) awaiting review.

## Activity

- Commits: 0 in 24h · 0 in 7d · 23 in 30d.
- Authors: 0 in 24h · 0 in 7d · 3 in 30d.
- 24h / 7d line churn: 0 (no commits).
- 30d line churn: +1,639 / −89 — file-type mix: 16 `.md`, 12 `.py`, 7 `.yml`,
  1 `.toml`; docs commits dominate volume, authored `.py` is a meaningful
  share (12 files vs. 16 `.md`).
- 30d commit types: 16 docs · 2 feat · 2 ci · 1 style · 1 fix · 1 build.

## System / Code Health

- Lint: ruff 0.16.0 — **54 errors** across 13 rule categories:
  BLE001:10, ISC004:8, TRY004:7, I001:5 (fixable), PYI034:4, PYI036:4
  (fixable), G201:3, RUF100:3 (fixable/stale-noqa), SIM102:3, C400:2,
  C401:2, PLR0402:2 (fixable), PERF402:1. All categories absent from the
  8-rule CI baseline (#121); version mismatch with CI's ruff is the most
  likely cause. Note: I001 still appears (5 occurrences) despite #121's
  import-sort fix — consistent with a different sorting algorithm in 0.16.0.
- Tests: 126 collected (4.10s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 79 collection errors all trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- CI status: unavailable — GitHub MCP actions API not accessible in this env.
  Last known-good: #121 merge (2026-07-05), commit message confirmed clean
  ruff, mypy, and full-suite gates.
- Open PRs: 22 total — 19 draft pulse PRs (#124–#143, 2026-07-07 to
  2026-07-25) unmerged since #121; 2 dependabot CI bumps (#137
  `setup-python` 6→7, #138 `pypa/gh-action-pypi-publish` 1.14.0→1.14.1).
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (9 TODO, 1 XXX — unchanged for
  multiple pulses).

## Followups

1. **Ruff version drift — 54 local errors vs. clean CI baseline:** ruff 0.16.0
   flags 13 rule categories (BLE001, ISC004, TRY004, I001, …) absent from the
   8-rule set cleared in #121. Verify the version CI pins; if ruff has been
   upgraded upstream, a targeted lint-fix PR is needed before the next
   substantive commit lands in CI.
2. **Pulse PR accumulation at 19 unmerged (#124–#143):** 21 days of daily
   pulse runs have stacked open PRs with no merge or close. An auto-merge
   policy or periodic batch-close for `pulse/*` branches would prevent
   unbounded accumulation.
3. **2 dependabot CI bumps idle (#137, #138):** `setup-python` 6→7 and
   `gh-action-pypi-publish` 1.14.0→1.14.1 require no code changes — review
   and merge when convenient to keep CI actions current.
4. **TODO plateau (10 markers, stable across many pulses):** 9 TODOs + 1 XXX
   in `src/` unchanged. Worth a triage pass once main-branch activity resumes
   to separate intentional deferrals from addressable debt.
