# Repo-Health Pulse — 2026-06-04 15:42 UTC

**Repository:** constitutional-swarm (standalone) | **Window:** trailing 24 h (buffer −15 min)

---

## Headlines

- Main branch all-green: 4/4 CI workflows passed on the latest push (2026-06-04T05:17Z).
- Lint clean; 124 tests collect locally — 76 collection errors are uniform `acgs_lite` missing-dep, expected in standalone env.
- 24 h churn dominated by a single bulk vendor commit (184 agent-persona `.md` templates, ~58 k lines); authored code delta is ~1,250 lines.

---

## Activity

| Window | Commits | Distinct authors |
|--------|---------|-----------------|
| 24 h   | 8       | 1               |
| 7 d    | 38      | 3               |
| 30 d   | 74      | 4               |

**24 h commit-type prefixes:** feat×3, fix×2, refactor×1, docs×1, chore×1

**30 d commit-type prefixes:** feat×25, build×10, docs×9, fix×7, chore×6, ci×2, refactor×1

**24 h line churn:** +59,283 / −516
> ⚠ BULK VENDOR FLAG — commit `614111294361` vendored 184 agent-persona templates
> (233 `.md` files via `chore(agents): vendor persona agent template library`).
> Stripping this single commit, net authored delta ≈ +1,250 / −516 across 14 `.py` files,
> 3 `.yaml`, 2 `.toml`, 2 `.yml`, and config files.

**30 d line churn:** +88,846 / −1,747 (contains the same bulk import; 30 d py/md split skewed accordingly)

---

## System / Code Health

### CI (GitHub Actions — via MCP, page 1 of ~21 pages, 30 most-recent runs)

| Workflow            | Latest conclusion | Branch | Timestamp (UTC)       |
|---------------------|-------------------|--------|-----------------------|
| CI                  | SUCCESS           | main   | 2026-06-04T05:17:38Z  |
| agent-check         | SUCCESS           | main   | 2026-06-04T05:17:38Z  |
| security-regression | SUCCESS           | main   | 2026-06-04T05:17:38Z  |
| verify-citations    | SUCCESS           | main   | 2026-06-04T05:17:38Z  |

All 30 sampled runs (24 h window): success=30, failure=0, cancelled=0.

**Open PRs:** 0

### Lint

`ruff check src/` → **PASS** (0 violations)

### Test collection (local, no full run — CI is run-of-record)

- Collected: **124 tests**
- Collection errors: **76** — all share one root cause: `ModuleNotFoundError: No module named 'acgs_lite'`
  (propagates via `constitutional_swarm/__init__.py → bench.py → dna.py`).
  This is expected in the standalone repo env without the ACGS monorepo installed.
  CI matrix (1,503 / 1,652 passing per CLAUDE.md) runs the full suite with the dep present.

### Debt markers in `src/`

| Marker | Count |
|--------|-------|
| TODO   | 9     |
| XXX    | 1     |
| FIXME  | 0     |
| HACK   | 0     |
| **Total** | **10** |

---

## Followups

1. **Collection errors (76)** — all trace to `acgs_lite` absent in standalone install.
   Verify CI matrix installs it (or marks affected tests optional via `pytest.importorskip`);
   add a note to local dev setup docs so contributors don't hit a 76-error wall on first run.

2. **Bulk vendor in churn metrics** — the 184-template commit makes raw churn numbers
   misleading. Consider tagging bulk-import commits with a `vendor:` prefix or tracking
   authored-code line delta separately in future pulses.

3. **Zero open PRs** — consistent with a squash-to-main or merge-on-green workflow.
   No action needed unless the team expects PR-based review gates.

4. **`acgs_lite` import in `__init__.py`** — the unconditional top-level import means any
   consumer of the package without the monorepo installed hits an error on `import constitutional_swarm`.
   Worth gating behind a try/except or moving to a lazy import if standalone use is intended.
