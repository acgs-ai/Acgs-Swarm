# Repo Health Pulse — constitutional-swarm

**Window:** 2026-07-23 12:00 → 2026-07-24 11:59 UTC (24h, 15-min trailing buffer)
**Run:** 2026-07-24 12:14 UTC · branch `main`

> Note: this repo has no product telemetry (no analytics/tracing/payments, no
> deployed surface). This is a **repo-health** read substituted for the usual
> user-experience pulse — git activity, CI, and lint/test status, not real usage.

## Headlines

- **19-day code quiet:** no commits since 2026-07-05 (SHA `1ecc0dfc0f`, commit #121). The
  30d window contains 23 commits from the burst ending on that date; the 7d and 24h
  windows are both empty.
- **Lint regression:** `ruff 0.16.0` (resolved from the loose `>=0.4` pin) now reports
  54 errors against `src/` — up from 0 at the time of commit #121, which cleared an
  8-error baseline under an older ruff version. Top rules: RUF001 (13), BLE001 (10),
  ISC004 (8), TRY004 (7), I001 (5).
- **CI mixed on main:** `verify-citations` failed on main's latest SHA (`1ecc0dfc0f`) on
  2026-07-20 — the most recent run of a core project workflow; dependency-bot runs
  (uv/GitPython) are also failing on main in the 24h window.

## Activity

- Commits: 0 in 24h · 0 in 7d · 23 in 30d.
- Authors: 0 in 24h · 0 in 7d · 3 in 30d.
- 24h line churn: none (no commits in window).
- 30d line churn: +1,639 / −89 — file-type mix: 16 `.md`, 12 `.py`, 7 `.yml`,
  1 `.toml`; authored `.py` is a meaningful share of 30d total; no bulk vendored drops
  observed.
- 30d commit types: 16 docs · 2 feat · 2 ci · 1 style · 1 fix · 1 build.

## System / Code Health

- Lint: `ruff check src/` — **54 errors** (14 auto-fixable; 16 more with
  `--unsafe-fixes`). Top offenders by rule: RUF001×13 (ambiguous unicode),
  BLE001×10 (blind `except Exception`), ISC004×8 (implicit string concat),
  TRY004×7 (wrong exception type), I001×5 (import sort). Errors reflect ruff 0.16.0
  new-rule coverage vs. the `>=0.4` lower-bound pin — not new code additions since
  last commit.
- Tests: 126 collected (3.35s). Not run locally; CI is the run-of-record.
  - *Expected-env caveat:* 79 collection errors all trace to absent `acgs_lite`
    (standalone remote env; not a repo problem).
- CI runs in-window (24h): 5 total, **5 failure**, 0 success.
  - 2 main-branch failures: "uv in /. for GitPython" (automated dependency-update
    bot; 2026-07-24T10:58 and 08:43 UTC).
  - 3 `pulse/2026-07-23` branch failures: `CI`, `agent-check`, `verify-citations`
    (pulse PRs' CI consistently red — same pattern as prior pulse branches).
- Latest main-branch core CI: `verify-citations` — **failure** — 2026-07-20T08:01
  UTC on SHA `1ecc0dfc0f`. Other same-SHA runs on 2026-07-20: 3 Dependabot-class
  successes (`github_actions`, `pip`). Primary `CI` and `security-regression`
  workflows not observed in the 60-run list beyond the initial 2026-07-05 merge
  window.
- Open PRs: **20** — 18 draft/ready `pulse/*` reports (spanning 2026-07-06 → 2026-07-23,
  i.e. 48 days of unmerged pulse PRs); 2 ready Dependabot PRs (#137
  `actions/setup-python 6→7`, #138 `pypa/gh-action-pypi-publish 1.14.0→1.14.1`,
  both open since 2026-07-20); 1 build bump (#122, open since 2026-07-06).
- **Dependabot security alerts on default branch:** 28 total (12 high, 10 moderate,
  6 low) reported by GitHub on push. See repo Security tab for details.
- Working tree: clean.
- TODO/FIXME/XXX/HACK markers in `src/`: 10 (9 TODO, 1 XXX — unchanged for 3+ pulses).

## Followups

1. **`verify-citations` failing on main's latest SHA:** The last observed run of this
   core workflow (2026-07-20) returned failure on `1ecc0dfc0f`. With no code pushes
   since 2026-07-05, the failure is either a stale external reference in docs/paper or
   a workflow environment issue. Worth a targeted re-run or log inspection to
   distinguish the two before the next code push.
2. **Lint ruff 0.16.0 divergence (54 errors):** The `>=0.4` lower bound resolves to
   ruff 0.16.0 in the remote env, which enables BLE001, ISC004, TRY004, PYI036/34 and
   others not active when #121 was committed. 14 errors are auto-fixable. Consider
   either pinning ruff (e.g. `ruff>=0.4,<0.17`) or running a follow-up `ruff check
   --fix` pass — otherwise CI ruff steps will diverge from local tooling depending on
   install time.
3. **Pulse PR backlog (18 open, oldest from 2026-07-06):** Every pulse since the
   last-pulse observation (#114, #115) has opened a new draft PR with no merge or
   close. At 18 open pulse PRs, triage cost is accumulating. A bulk-close of stale
   drafts and a scheduled auto-merge (or explicit no-PR) policy for `pulse/*` branches
   would prevent unbounded growth.
4. **28 Dependabot security alerts on default branch (12 high):** Surfaced on push.
   The 19-day code quiet means these alerts have gone unaddressed. Recommend reviewing
   the Security tab and triaging the 12 high-severity findings; the 2 ready Dependabot
   PRs (#137, #138) cover CI tooling only and are low risk to merge immediately.
5. **19-day code quiet — two unmerged Dependabot PRs:** #137 and #138 (both CI
   workflow bumps, "ready" state, 4 days open) are the only non-pulse open PRs. With
   no other code activity, merging these is low risk and keeps the dependency graph
   current.
